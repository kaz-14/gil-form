.draft-content
  .screan__content
    .screan__text
      %h1 下書き一覧
  .contents.row
    - @tweets.each do |tweet|
      - if current_user.id == tweet.user.id
        = render partial: "tweet", locals: { tweet: tweet }