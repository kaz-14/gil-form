.contents.row
  .container
    = form_with(model: @tweet, local: true) do |form|
      %h3
        編集する
      = form.text_field :image, placeholder: "写真URL"
      = form.text_area :text, placeholder: "文章" , rows: "10"
      = form.submit "SEND"